{
  "name": "class-scheduling",
  "private": "true",
  "scripts": {
    "start": "concurrently \"wsrun --parallel start\"",
    "build": "wsrun --parallel build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^8.2.0",
    "wsrun": "^5.2.4"
  }
}
