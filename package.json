{
  "name": "class-scheduling",
  "private": true,
  "scripts": {
    "start": "dotenv -- concurrently \"wsrun --parallel start\""
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^8.2.0",
    "dotenv-cli": "^7.3.0",
    "wsrun": "^5.2.4"
  }
}
